{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",
    "paths": {
      // expo doesn't allow @ to be used for some reason
      "pages": ["./pages"]
    }
  }
}
